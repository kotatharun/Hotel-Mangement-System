## Git & Documentation

This project uses Git for version control with incremental commits to track development progress. The repository contains more than 5 commits covering database setup, debugging, CRUD implementation, UI improvements, and normalization updates.

A `.gitignore` file is included to exclude unnecessary and sensitive files such as:

- venv/ (virtual environment)
- __pycache__/ (Python cache files)
- *.pyc (compiled Python files)
- .env (environment variables)

---

## Git Commit History

- Initial Flask setup with database connection  
- Fix database connection and data rendering  
- Implement CRUD operations for Guests and Reservations  
- Improve UI with Bootstrap styling  
- Update normalization report with decomposition  
- Add git documentation and .gitignore  
